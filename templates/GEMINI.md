# AI Orchestrator

This project uses the AI Orchestrator toolkit installed at `{{AI_ORCHESTRATOR_DIR}}/`.

When asked to provide consultation or generate assets for another agent, follow the constraints in:

```text
{{AI_ORCHESTRATOR_DIR}}/AGENT_RULES.md
```

Do not edit source code unless the user explicitly asks for an execution workflow. For image generation, save only the requested output asset.
